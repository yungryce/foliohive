# Test requirements for Cloudfolio integration tests
# Note: cloudfolio-shared is installed via setup-dev.sh in editable mode

# Testing framework
pytest>=8.0.0

# HTTP mocking (for GitHub API tests)
responses>=0.25.0

# Azure Functions (for function app tests)
azure-functions>=1.17.0
